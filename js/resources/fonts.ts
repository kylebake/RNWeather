import { PixelRatio } from 'react-native'

export const Fonts = {
  size10: 10 / PixelRatio.getFontScale(),
  size11: 11 / PixelRatio.getFontScale(),
  size12: 12 / PixelRatio.getFontScale(),
  size14: 14 / PixelRatio.getFontScale(),
  size18: 18 / PixelRatio.getFontScale(),
  size20: 20 / PixelRatio.getFontScale(),
  size22: 22 / PixelRatio.getFontScale(),
  size24: 24 / PixelRatio.getFontScale(),
}
